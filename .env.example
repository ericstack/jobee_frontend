# Base URL of the backend API (include the /api prefix)
VITE_API_URL=http://localhost:3000/api

# Base URL where uploaded files (avatars/resumes) are served by the backend
VITE_UPLOADS_URL=http://localhost:3000/uploads
